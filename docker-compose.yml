version: '3.8'

services:
  tsd_monitor:
    build: .
    volumes:
      - ./logs:/app/logs
      - ./models:/app/models
      - ./reports:/app/reports
    # Note: On Linux, you might need --privileged or device flags for hardware access
    # but for this demo, simulation mode is preferred for Docker.
    command: simulate
